# Optional local development CORS proxy. Disabled unless explicitly allowlisted.
# HTTPS origins only, exact match, comma-separated, no trailing slash.
# Never allow untrusted/internal destinations; never expose the dev server publicly.
TAG_MASTER_PROXY_ORIGINS=https://api.openai.com,https://generativelanguage.googleapis.com
